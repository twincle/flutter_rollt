part of weather;

class HourlyWidget extends StatelessWidget {
  final Map<String, dynamic> temp;

  const HourlyWidget({super.key, required this.temp});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
